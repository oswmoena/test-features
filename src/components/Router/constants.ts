export enum PATHS {
    NOTIFICATION = "/notification",
    CALENDAR = "/calendar",
    DEBOUNCE = "/debounce",
    KEMPO_CARDS = "/kempo-cards",
    REACT_QUERY = "/react-query",
    POKEMON = "/pokemon",
}